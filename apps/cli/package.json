{
  "name": "@aaroncx/monad",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "bun build --compile --outfile dist/monad src/main.ts"
  }
}
